(car ''abracadabra)

;; Explanation
;(the interpreter interprets the above expression as 
;as a string literal whose first character is ', because of the first quote 
;the remaining of the expression is itself treated as non-numeral)